{
	"scripts": {
		"start": "bun run src/index.ts",
		"block-spam-accounts": "bun run src/block-spam-accounts.ts",
		"follow-back": "bun run src/follow-back.ts",
		"format": "bunx biome format --write && bunx prettier --write README.md",
		"lint": "bunx biome lint --write",
		"check": "bunx biome check --write && bunx prettier --write README.md"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.8",
		"@types/bun": "^1.3.11",
		"prettier": "^3.8.1"
	},
	"peerDependencies": {
		"typescript": "^6.0.2"
	},
	"dependencies": {
		"@octokit/rest": "^22.0.1",
		"handlebars": "^4.7.8"
	}
}
